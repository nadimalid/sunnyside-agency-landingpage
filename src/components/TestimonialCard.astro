---
const {avatar, content} = Astro.props
---

<div class="flex flex-col items-center gap-y-10 font-barlow xl:gap-y-0">
    <img class="w-16 rounded-full" src={content.avatar} alt="The persons image">
    <p class="max-w-[32ch] text-very-dark-grayish-blue text-center md:max-w-[34ch] xl:pt-16 xl:pb-20">{content.testimony}</p>
    <div class="flex flex-col items-center gap-y-1">
        <span class=" font-fraunces font-black text-lg text-dark-desaturated-blue">{content.name}</span>
        <span class="text-grayish-blue">{content.position}</span>
    </div>
</div>